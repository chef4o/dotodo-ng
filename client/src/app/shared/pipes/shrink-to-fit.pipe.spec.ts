import { ShrinkToFitPipe } from './shrink-to-fit.pipe';

describe('ShrinkToFitPipe', () => {
  it('create an instance', () => {
    const pipe = new ShrinkToFitPipe();
    expect(pipe).toBeTruthy();
  });
});
